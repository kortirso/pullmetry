class UpdateCompaniesUsers < ActiveRecord::Migration[7.1]
  def up
    # update_service = Persisters::Invites::AcceptService.new
    # Invite.where(code: nil).each do |invite|
    #   user = invite.receiver
    #   update_service.call(invite: invite, user: user)
    # end
  end
end
