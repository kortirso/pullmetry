ru:
  dry_validation:
    rules:
      title: Заголовок
      name: Название
      external_id: Внешний ID
    errors:
      company:
        rules:
          title:
            filled?: не может быть пустым
          name:
            filled?: не может быть пустым
      repository:
        rules:
          external_id:
            empty: не может быть пустым
