ru:
  dry_validation:
    rules:
      title: Заголовок
      name: Название
      external_id: Внешний ID
      entity_value: Entity value
      excludes_rule: Исключение
      link: Ссылка
    errors:
      subscriber:
        rules:
          email:
            invalid: неверный формат
      company:
        rules:
          title:
            filled?: не может быть пустым
          name:
            filled?: не может быть пустым
      repository:
        rules:
          external_id:
            empty: не может быть пустым
          link:
            invalid_host: должна начинаться с https://github.com или https://gitlab.com
      ignore:
        rules:
          entity_value:
            filled?: не может быть пустым
