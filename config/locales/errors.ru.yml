ru:
  dry_validation:
    rules:
      title: Заголовок
      name: Название
    errors:
      company:
        rules:
          title:
            filled?: не может быть пустым
          name:
            filled?: не может быть пустым
      user:
        rules:
          email:
            filled?: не может быть пустым
            invalid: имеет неверный формат
