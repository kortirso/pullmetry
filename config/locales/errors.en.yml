en:
  dry_validation:
    rules:
      external_id: External id
    errors:
      company:
        rules:
          title:
            filled?: can't be blank
          name:
            filled?: can't be blank
      repository:
        rules:
          external_id:
            empty: can't be blank
